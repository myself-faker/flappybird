function aa(){
    a=1
    console.log(a);
}
aa();
k=[
    ['wo','肖驰']
]
var o=new Map(k);
console.log(o[1]);

// 基础 对象
// Object String Number NUll undefined boolean 

// 对象下的对象
// Object Function Math RegExp  Date Array Golbal JSON Error

// 最新的
// Symbol Promise


// 小鸟 学习
// Map 对象    (例:将二维数组 转化为 Map{arr[0][0] => arr[0][1]}) 的 对象  通过for(let [k,v] of MAP ) 遍历对象组 在通过  set方法(key,value),形成键值对
// Image 对象(以前可能没注意) 一个图片


//需巩固 画布的知识